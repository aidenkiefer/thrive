// Staff / team page
// Data: staff table ordered by display_order

export const revalidate = 60

export default function TeamPage() {
  return (
    <div>
      {/* TODO: Staff grid — photo, name, title, bio */}
      <h1>Our Team</h1>
    </div>
  )
}
